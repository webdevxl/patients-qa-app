# PostgreSQL connection used by the backend (Prisma) and tooling.
# Matches the credentials in docker-compose.yml.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/patients_qa?schema=public"

# LLM provider key — only needed once the LangChain agent is implemented (later step).
# Leave blank for the current scaffold + seed milestone.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
