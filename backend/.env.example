# Copy to .env before running the backend.
DATABASE_URL=

# Secret used to sign cohort session tokens (build-out plan #1). REQUIRED — the backend
# refuses to start without it. Use a long random string, e.g. `openssl rand -hex 32`.
JWT_SECRET=
# Optional token lifetime (any jsonwebtoken duration string). Defaults to 12h.
# JWT_EXPIRES_IN=12h

# LLM provider key — only needed once the LangChain agent is implemented (later step).
# OPENAI_API_KEY=

# Injection-guard classifier (opt-in). When set, every Q&A request first runs through a small
# OpenAI model that flags prompt-injection attempts (system_prompt_override) and cross-cohort
# access attempts before reaching the bigger find/answer model. Unset ⇒ the guard is a no-op
# (zero tokens, zero latency), and behavior matches the pre-guard baseline — useful in dev/test.
# Verdict is recorded on every request in `request_log.guard_*` regardless.
# OPENAI_GUARD_MODEL=gpt-5-nano
# OPENAI_GUARD_TEMPERATURE=0

# LangSmith tracing — when LANGSMITH_TRACING=true, every LangChain agent run is
# automatically traced to LangSmith (no code changes needed). Get a key at
# https://smith.langchain.com → Settings. LANGSMITH_PROJECT groups the traces.
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=
# LANGSMITH_PROJECT=patients-qa
